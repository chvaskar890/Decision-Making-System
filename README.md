This project is a web-based implementation of the Analytic Hierarchy Process (AHP) for decision-making.
It allows users to define criteria, compare them pairwise, input scores for alternatives, and calculate the priority weights and final rankings automatically.

The app replicates the exact logic from the original C program but with a JavaScript + HTML + CSS frontend, making it easy to use directly in a browser without additional setup.
